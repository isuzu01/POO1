@model IEnumerable<POO1_Tarea03_MVC.Models.Alumno>

@{
    ViewBag.Title = "Lista de Alumnos";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Agregar Nuevo Alumno", "Crear")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Alumno_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Alumno_nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Edad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Alumno_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Alumno_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Edad)
        </td>
        <td>
            @Html.ActionLink("Editar", "Editar", new {  id=item.Alumno_id  }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
